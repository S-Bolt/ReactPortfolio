import React from "react";
import Navigation from "./Navigation";
 
const Footer = () => {
    return(
        <header>
            <h1> My Portfolio</h1>
            <Navigation />
        </header>
    );
};

export default Footer;